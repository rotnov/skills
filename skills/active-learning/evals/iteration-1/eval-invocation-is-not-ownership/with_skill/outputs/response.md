# Response

Do not assign ownership to data-export merely because it ran. Compare the correction
against the startup workflow and active-learning responsibilities; both are plausible,
so present the evidence and ask the user to select or refine the boundary. Propose the
lesson-to-owner change before any write.
